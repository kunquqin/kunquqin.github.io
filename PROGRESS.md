# 当前进度

更新：2026-09-06。当前主题：iPhone 17 Pro 与小米 SU7 项目页局部优化。

## 已完成

- `themes/portfolica/layout/iphone-render.ejs`：删除首屏小字“全新一体成型设计，把专业性能装进更纯粹的轮廓。”。
- `themes/portfolica/source/css/style.css`：第二屏 `.ip17-unibody-visual img` 增加 0.7 秒 transform 过渡；精细指针且支持 hover 时放大至 1.1，移开恢复；尊重减少动态效果偏好。
- 用户反馈放大时投影裁切；将 `.ip17-unibody-visual.ip17-reveal.is-visible` 的 `clip-path` 和 `filter` 改为 `none`，解除显示状态下外层裁切。
- `themes/portfolica/layout/su7.ejs`：删除七个主区块标题上方的编号英文小字（02–08），保留区块标题、正文与内部模块标签。
- `themes/portfolica/layout/su7.ejs` 与 `themes/portfolica/source/css/su7.css`：重构车色区为全宽无圆角图片；主标题改为“臻选车色　自成风景”并居中；车色名称与描述置于图片左下，色块置于右下并隐藏色名；说明文字移至图片下方居中并淡化。主区块标题统一字级和字距。
- 车色区继续调整：主标题移入图片上方居中；当前车色名称移至右下色块上方并缩小、移除投影；隐藏车色描述；色块加大间距，取消选中外圈，选中时放大 12%。
- 修正车色区定位：标题强制在图片内水平居中并下移；色块工具栏强制右对齐，颜色名称随其右对齐；选中色块放大至 30%。
- 车色图片使用按滚动方向触发的自定义缓动居中：向下进入或向上返回时在更大范围内缓入缓出到中线；继续朝离开方向滚动不拉回，触摸、鼠标或按键会立即取消。标题位于图片高度 12%，色块工具栏距底 15%。
- 顶部“设计”导航使用稳定的设计区锚点定位，避免轮播高度变化时二次点击发生累计偏移；设计区专用滚动偏移设为 74px，使主图接近视口中线并保留“流畅侧影”。SU7 页面资源缓存版本更新为 `20260906b`。
- 已在 `localhost:4000` 实测连续点击两次“设计”：外观轮播顶端位置稳定在 277–279px，未再发生第二次点击下移。
- 选中车色圆点保留同色细外圈，取消缩放；CSS 缓存版本更新为 `20260906d`。
- 车色标题恢复为图片内顶部居中；CSS 缓存版本更新为 `20260906f`。
- 已建立项目 AGENTS.md 与本进度文件；通用协作及自动维护项目进度的规则已保存到 `C:/Users/Administrator/.codex/AGENTS.md`，供本机其他项目复用。

## 验证与待办

- 上述页面修改后 `npm.cmd run build` 通过。
- 投影裁切修复尚未进行浏览器视觉验证，也未收到用户确认；继续相关任务时优先核实。
- 当前没有其他已明确要求的页面修改。
- 本轮网站运行代码与所需素材将提交并推送；`docs/` 下的提示词、参考图与生成中间素材不纳入本次提交。
