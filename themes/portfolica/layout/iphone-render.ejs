<link rel="stylesheet" href="<%- url_for('/css/iphone-typography.css?v=20260904') %>">
<article class="ip17-product-page ip17-type-system">
  <header class="ip17-product-hero" id="ip17-top" data-color="orange" data-view="back">
    <div class="ip17-hero-sticky">
    <a class="ip17-product-back" href="<%- url_for('/3d/#projects') %>">← 返回 3D 项目</a>
    <div class="ip17-product-title"><p>iPhone 17 Pro</p><h1>强大，<br>由内而外。</h1></div>
    <figure class="ip17-hero-product" tabindex="0" aria-label="按住并拖动手机" aria-live="polite">
      <div class="ip17-hero-aura"></div>
      <img class="is-active" draggable="false" data-product-color="orange" data-product-view="back" src="<%- url_for('/images/iphone17-pro/hero-cosmic-orange.png') %>" alt="星宇橙色 iPhone 17 Pro 背面">
      <img draggable="false" data-product-color="blue" data-product-view="back" src="<%- url_for('/images/iphone17-pro/hero-deep-blue.png') %>" alt="深蓝色 iPhone 17 Pro 背面">
      <img draggable="false" data-product-color="silver" data-product-view="back" src="<%- url_for('/images/iphone17-pro/hero-silver.png') %>" alt="银色 iPhone 17 Pro 背面">
      <img draggable="false" data-product-color="orange" data-product-view="front" src="<%- url_for('/images/iphone17-pro/front-cosmic-orange.png') %>" alt="星宇橙色 iPhone 17 Pro 正面">
      <img draggable="false" data-product-color="blue" data-product-view="front" src="<%- url_for('/images/iphone17-pro/front-deep-blue.png') %>" alt="深蓝色 iPhone 17 Pro 正面">
      <img draggable="false" data-product-color="silver" data-product-view="front" src="<%- url_for('/images/iphone17-pro/front-silver.png') %>" alt="银色 iPhone 17 Pro 正面">
    </figure>
    <div class="ip17-view-switch" role="group" aria-label="选择手机视角"><button type="button" data-view-option="front" aria-label="切换至正面" aria-pressed="false"><span>正</span><small>FRONT</small></button><button class="is-active" type="button" data-view-option="back" aria-label="切换至背面" aria-pressed="true"><span>背</span><small>BACK</small></button></div>
    <div class="ip17-product-colors" role="group" aria-label="选择机身颜色"><button class="orange is-active" type="button" data-color-option="orange" data-tooltip="星宇橙" aria-label="星宇橙" aria-pressed="true"></button><button class="blue" type="button" data-color-option="blue" data-tooltip="深蓝色" aria-label="深蓝色" aria-pressed="false"></button><button class="silver" type="button" data-color-option="silver" data-tooltip="银色" aria-label="银色" aria-pressed="false"></button></div>
    <div class="ip17-product-scroll">向下探索 <b>↓</b></div>
    </div>
  </header>
  <nav class="ip17-product-nav" aria-label="产品章节"><a href="#design">设计</a><a href="#display">显示屏</a><a href="#camera">影像</a><a href="#performance">性能</a><a href="#battery">续航</a></nav>
  <section class="ip17-product-intro" id="design"><h2>一体成型结构<br>更强大的 Pro。</h2><p class="ip17-copy">热锻铝金属一体成型机身，将性能、散热与握持感融入同一条连续曲线。横向延展的专业摄像平台，让内部空间得到更高效的利用。</p><figure class="ip17-design-stage ip17-unibody-visual"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/unibody.png') %>" alt="iPhone 17 Pro 一体成型机身结构展示"></figure></section>
  <section class="ip17-stat-band"><div><b>6.9</b><span>英寸<br>超视网膜 XDR 显示屏</span></div><div><b>120</b><span>Hz<br>ProMotion 自适应刷新率</span></div><div><b>3000</b><span>尼特<br>户外峰值亮度</span></div></section>
  <section class="ip17-display-section" id="display" data-sequence-state="idle">
    <div class="ip17-display-sticky">
      <div class="ip17-display-background" aria-hidden="true">
        <img class="ip17-display-bright" src="<%- url_for('/images/iphone17-pro/display/background-bright.webp') %>" alt="">
        <span class="ip17-display-shade"></span>
      </div>
      <div class="ip17-display-copy">
        <h2>阳光之下，<br>依然出色。</h2>
        <p class="ip17-copy">6.9 英寸超视网膜 XDR 显示屏，更高的户外峰值亮度与先进抗反射能力，让画面在强光环境下依然清晰鲜活。</p>
      </div>
      <figure class="ip17-screen-stage" aria-label="iPhone 17 Pro 显示屏在环境光变化下依然保持清晰">
        <img class="ip17-display-sequence" draggable="false" src="<%- url_for('/images/iphone17-pro/display/sequence/frame-000.webp') %>" data-frame-base="<%- url_for('/images/iphone17-pro/display/sequence/frame-') %>" data-frame-count="72" alt="iPhone 17 Pro 横屏播放高动态范围画面">
      </figure>
    </div>
  </section>
  <section class="ip17-camera-section" id="camera">
    <header><h2>八种焦段，<br>远近随心。</h2><p class="ip17-copy">从微距到 200 mm 长焦，取景自由切换。</p></header>
    <div class="ip17-camera-showcase"><div class="ip17-camera-keyfacts"><article><small>最高达</small><strong>8 倍</strong><span>光学品质变焦</span></article><article><small>全部</small><strong>4800 万像素</strong><span>后置摄像头</span></article></div><figure class="ip17-lens-closeup"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/hero-cosmic-orange.png') %>" alt="星宇橙 iPhone 17 Pro 背部摄像头模组特写"></figure></div>
    <div class="ip17-focal-viewer" data-active-focal="28mm">
      <figure class="ip17-focal-stage"><img src="<%- url_for('/images/iphone17-pro/focal-series/35mm.png') %>" alt="28 mm 焦段人像拍摄效果" data-focal-image></figure>
      <div class="ip17-focal-tags" role="group" aria-label="切换八种专业焦段">
        <button type="button" data-focal="13mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/13mm.png') %>" aria-pressed="false">13 mm</button>
        <button type="button" data-focal="24mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/24mm.png') %>" aria-pressed="false">24 mm</button>
        <button class="is-active" type="button" data-focal="28mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/35mm.png') %>" aria-pressed="true">28 mm</button>
        <button type="button" data-focal="35mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/28mm.png') %>" aria-pressed="false">35 mm</button>
        <button type="button" data-focal="48mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/48mm.png') %>" aria-pressed="false">48 mm</button>
        <button type="button" data-focal="100mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/100mm.png') %>" aria-pressed="false">100 mm</button>
        <button type="button" data-focal="200mm" data-src="<%- url_for('/images/iphone17-pro/focal-series/200mm.png') %>" aria-pressed="false">200 mm</button>
        <button type="button" data-focal="macro" data-src="<%- url_for('/images/iphone17-pro/focal-series/macro.png') %>" aria-pressed="false">微距</button>
      </div>
    </div>
  </section>
  <section class="ip17-photo-results" aria-labelledby="ip17-results-title">
    <div class="ip17-results-heading"><h3 id="ip17-results-title">每一处细节，都有表现力。</h3><p>从暗部层次，到鲜活色彩。</p></div>
    <div class="ip17-results-track" tabindex="0" role="region" aria-label="摄影效果画廊，可左右滑动">
      <% const photoResults = [
        ['low-light', '暗下来，层次还在。', '低光影像', '保留建筑纹理与人物轮廓，让光与暗各有表达。', '青色低光建筑中身穿赭橙外套的男模特'],
        ['portrait-detail', '细节，经得起细看。', '人像细节', '从卷发到织物褶皱，让细腻质感成为画面主角。', '铜色卷发女模特与黄绿色褶皱丝织衣领'],
        ['ultra-wide-v3', '换个视角，张力全开。', '超广角视野', '拉开空间的纵深，捕捉肢体与建筑之间的张力。', '贴地仰拍向镜头跨步的黑白人物，前脚与高耸建筑呈现强烈近远透视'],
        ['color-style', '色彩，自有态度。', '色彩表达', '暖色与冷色相遇，留下鲜明而自然的人像氛围。', '琥珀色装置前身穿朱红西装的短发女模特'],
        ['still-life', '静物，也能鲜活。', '静物质感', '用一束侧光，呈现果皮、果肉与细小水珠的层次。', '深绿色石台上的新鲜无花果与切面']
      ]; photoResults.forEach((photo) => { %>
      <figure class="ip17-results-card"><img loading="lazy" decoding="async" width="1122" height="1402" src="<%- url_for('/images/iphone17-pro/photo-results/' + photo[0] + '.png') %>" alt="<%= photo[4] %>"><figcaption><small><%= photo[2] %></small><strong><%= photo[1] %></strong><p><%= photo[3] %></p></figcaption></figure>
      <% }); %>
    </div>
    <div class="ip17-results-controls"><div class="ip17-results-dots" aria-label="选择摄影作品"></div><button type="button" data-results-prev aria-label="上一张摄影作品">←</button><button type="button" data-results-next aria-label="下一张摄影作品">→</button></div>
  </section>
  <script src="<%- url_for('/js/iphone-photo-results.js') %>" defer></script>
  <section class="ip17-life-section">
    <div class="ip17-life-copy"><h2>动起来，<br>依然稳得漂亮。</h2><p class="ip17-copy">更强的视频防抖与电影级规格，让高速运动、低光场景和专业工作流都能保持清晰、自然、有层次。</p></div>
    <div class="ip17-motion-showcase">
      <div class="ip17-motion-gallery" aria-label="运动视频拍摄示例">
        <figure class="ip17-motion-primary"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/action-stabilization.png') %>" alt="高速奔跑中的人物保持清晰，背景呈现自然运动感"><figcaption><strong>4K 120 fps 杜比视界</strong><span>高速运动，也能清晰定格。</span></figcaption></figure>
        <figure><img loading="lazy" src="<%- url_for('/images/iphone17-pro/action-bmx.png') %>" alt="BMX 运动员高速过弯的稳定影像"></figure>
        <figure><img loading="lazy" src="<%- url_for('/images/iphone17-pro/action-skateboard.png') %>" alt="夜间滑板运动的稳定影像"></figure>
        <figure><img loading="lazy" src="<%- url_for('/images/iphone17-pro/action-dance.png') %>" alt="建筑空间中双人舞蹈的动态影像"></figure>
        <figure><img loading="lazy" src="<%- url_for('/images/iphone17-pro/action-coastal-run.png') %>" alt="海岸跑步与宠物追拍的动态影像"></figure>
      </div>
      <div class="ip17-motion-controls" aria-label="切换拍摄示例"><button type="button" data-motion-prev aria-label="上一张">←</button><button type="button" data-motion-next aria-label="下一张">→</button></div>
    </div>
  </section>
  <section class="ip17-performance-section" id="performance"><div class="ip17-performance-copy"><h2>持续强劲，<br>不止一瞬间。</h2><p class="ip17-copy">激光焊接均热板与铝金属一体成型结构协同导热，让高负载游戏、影像处理与本地 AI 任务保持稳定输出。</p><div class="ip17-performance-specs"><span><b>6 核</b>CPU</span><span><b>6 核</b>GPU</span><span><b>40%</b>持续性能提升</span></div></div><figure class="ip17-gameplay"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/gameplay.png') %>" alt="iPhone 17 Pro 横屏游戏画面"></figure></section>
  <section class="ip17-battery-section" id="battery"><div class="ip17-battery-copy"><h2>续航再进化。</h2><p class="ip17-copy">更高效的内部布局为电池释放更多空间，配合 A19 Pro 的能效表现，从工作到娱乐都能持续在线。</p></div><div class="ip17-battery-stats"><article><small>最长可达</small><strong>31 小时</strong><span>视频播放</span></article><article><small>最长可达</small><strong>28 小时</strong><span>流媒体视频播放</span></article><article><small>最多充至 50% 电量</small><strong>20 分钟</strong><span>搭配 40W 或更高功率适配器</span></article></div>
    <div class="ip17-battery-day" aria-label="从早到晚的手机生活场景">
      <% [['morning-v3', '出发', '短发女性在晨间列车窗边微笑查看橙色手机'], ['afternoon-v2', '探索', '同一女性午后手持橙色手机漫步街区'], ['evening-v2', '尽兴', '同一女性夜晚在露台咖啡馆用橙色手机通话']].forEach(([file, label, alt]) => { %>
      <figure><img loading="lazy" decoding="async" width="1122" height="1402" src="<%- url_for('/images/iphone17-pro/battery-day/' + file + '.png') %>" alt="<%= alt %>"><figcaption><%= label %></figcaption></figure>
      <% }); %>
    </div>
  </section>
  <section class="ip17-finale"><h2>所需的一切，<br>尽在 iPhone。</h2><div class="ip17-finale-lineup"><figure class="blue"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/finale/iphone17pro-front-deep-blue-side.png') %>" alt="深蓝色 iPhone 17 Pro 正面侧视图"></figure><figure class="orange"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/finale/iphone17pro-front-cosmic-orange.png') %>" alt="星宇橙 iPhone 17 Pro 正面图"></figure><figure class="silver"><img loading="lazy" src="<%- url_for('/images/iphone17-pro/finale/iphone17pro-front-silver-side.png') %>" alt="银色 iPhone 17 Pro 正面侧视图"></figure></div></section>
  <nav class="case-next ip17-next"><span>NEXT PROJECT</span><a href="<%- url_for('/projects/dewu/') %>">得物 3D 数字资产升级 →</a></nav>
</article>
<script>
(() => {
  const page = document.querySelector('.ip17-product-page');
  if (!page || matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  const items = page.querySelectorAll('h2, .ip17-copy, .ip17-product-intro .ip17-eyebrow, .ip17-camera-section .ip17-eyebrow, .ip17-focal-viewer, .ip17-camera-keyfacts article, .ip17-lens-closeup, .ip17-unibody-visual, .ip17-stat-band > div, .ip17-camera-cards article, .ip17-motion-gallery figure, .ip17-performance-specs, .ip17-battery-stats article, .ip17-battery-day figure, .ip17-finale-lineup figure');
  items.forEach((item) => item.classList.add('ip17-reveal'));
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.16, rootMargin: '0px 0px -8% 0px' });
  items.forEach((item) => observer.observe(item));
})();
(() => {
  const viewer = document.querySelector('.ip17-focal-viewer');
  if (!viewer) return;
  const image = viewer.querySelector('[data-focal-image]');
  const buttons = Array.from(viewer.querySelectorAll('[data-focal]'));
  const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;
  const labels = { macro: '微距', '13mm': '13 mm', '24mm': '24 mm', '28mm': '28 mm', '35mm': '35 mm', '48mm': '48 mm', '100mm': '100 mm', '200mm': '200 mm' };
  buttons.forEach((button) => { const preload = new Image(); preload.src = button.dataset.src; });
  const selectFocal = (button) => {
    if (button.classList.contains('is-active')) return;
    const focal = button.dataset.focal;
    viewer.dataset.activeFocal = focal;
    buttons.forEach((item) => {
      const active = item === button;
      item.classList.toggle('is-active', active);
      item.setAttribute('aria-pressed', String(active));
    });
    const swap = () => {
      image.src = button.dataset.src;
      image.alt = `${labels[focal]} 焦段拍摄效果`;
      requestAnimationFrame(() => image.classList.remove('is-switching'));
    };
    if (reduceMotion) swap();
    else {
      image.classList.add('is-switching');
      setTimeout(swap, 220);
    }
  };
  buttons.forEach((button) => button.addEventListener('click', () => selectFocal(button)));
})();
(() => {
  const hero = document.querySelector('.ip17-product-hero');
  if (!hero) return;
  const buttons = hero.querySelectorAll('[data-color-option]');
  const images = hero.querySelectorAll('[data-product-color]');
  const label = hero.querySelector('[data-color-label]');
  const names = { orange: '星宇橙', blue: '深蓝色', silver: '银色' };
  const syncProduct = () => images.forEach((image) => image.classList.toggle('is-active', image.dataset.productColor === hero.dataset.color && image.dataset.productView === hero.dataset.view));
  buttons.forEach((button) => button.addEventListener('click', () => {
    const color = button.dataset.colorOption;
    if (hero.dataset.color === color) return;
    hero.dataset.color = color;
    buttons.forEach((item) => {
      const active = item === button;
      item.classList.toggle('is-active', active);
      item.setAttribute('aria-pressed', String(active));
    });
    syncProduct();
    button.classList.add('suppress-tip');
    button.addEventListener('pointerleave', () => button.classList.remove('suppress-tip'), { once: true });
  }));
  const viewButtons = hero.querySelectorAll('[data-view-option]');
  viewButtons.forEach((button) => button.addEventListener('click', () => {
    hero.dataset.view = button.dataset.viewOption;
    viewButtons.forEach((item) => {
      const active = item === button;
      item.classList.toggle('is-active', active);
      item.setAttribute('aria-pressed', String(active));
    });
    syncProduct();
  }));
  const product = hero.querySelector('.ip17-hero-product');
  const sticky = hero.querySelector('.ip17-hero-sticky');
  const enterHero = () => requestAnimationFrame(() => requestAnimationFrame(() => sticky.classList.add('is-entered')));
  const openingImage = product.querySelector('img.is-active');
  if (openingImage.complete) enterHero();
  else {
    openingImage.addEventListener('load', enterHero, { once: true });
    setTimeout(enterHero, 900);
  }
  let ticking = false;
  const updateHero = () => {
    const start = matchMedia('(max-width: 600px)').matches ? 60 : 70;
    const range = Math.max(1, hero.offsetHeight - sticky.offsetHeight);
    const progress = Math.max(0, Math.min(1, (start - hero.getBoundingClientRect().top) / range));
    hero.style.setProperty('--hero-progress', progress.toFixed(3));
    const revealed = progress > 0.18;
    hero.classList.toggle('is-revealed', revealed);
    if (progress < 0.035 && (hero.dataset.color !== 'orange' || hero.dataset.view !== 'back')) {
      hero.dataset.color = 'orange';
      hero.dataset.view = 'back';
      buttons.forEach((item) => {
        const active = item.dataset.colorOption === 'orange';
        item.classList.toggle('is-active', active);
        item.setAttribute('aria-pressed', String(active));
      });
      viewButtons.forEach((item) => {
        const active = item.dataset.viewOption === 'back';
        item.classList.toggle('is-active', active);
        item.setAttribute('aria-pressed', String(active));
      });
      syncProduct();
    }
    ticking = false;
  };
  addEventListener('scroll', () => {
    if (!ticking) {
      requestAnimationFrame(updateHero);
      ticking = true;
    }
  }, { passive: true });
  addEventListener('resize', updateHero, { passive: true });
  updateHero();
  let dragStart = null;
  const releaseProduct = () => {
    dragStart = null;
    product.classList.remove('is-dragging');
    product.style.setProperty('--drag-x', '0px');
    product.style.setProperty('--drag-y', '0px');
  };
  product.addEventListener('pointerdown', (event) => {
    if (!hero.classList.contains('is-revealed')) return;
    dragStart = { x: event.clientX, y: event.clientY };
    product.setPointerCapture(event.pointerId);
    product.classList.add('is-dragging');
  });
  product.addEventListener('pointermove', (event) => {
    if (!dragStart) return;
    const x = event.clientX - dragStart.x;
    const y = event.clientY - dragStart.y;
    product.style.setProperty('--drag-x', x + 'px');
    product.style.setProperty('--drag-y', y + 'px');
  });
  product.addEventListener('pointerup', releaseProduct);
  product.addEventListener('pointercancel', releaseProduct);
})();
(() => {
  const section = document.querySelector('.ip17-display-section');
  if (!section) return;
  const sequence = section.querySelector('.ip17-display-sequence');
  const count = Number(sequence.dataset.frameCount) || 72;
  const base = sequence.dataset.frameBase;
  const frameUrl = (index) => `${base}${String(index).padStart(3, '0')}.webp`;
  const reducedMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;
  const frames = [];
  let ready = false;
  let played = false;

  const setSequenceProgress = (progress) => {
    const value = Math.max(0, Math.min(1, progress));
    section.style.setProperty('--display-sequence-progress', value.toFixed(3));
  };

  const setLayoutProgress = (progress) => {
    const value = Math.max(0, Math.min(1, progress));
    const eased = value < .5 ? 4 * value * value * value : 1 - Math.pow(-2 * value + 2, 3) / 2;
    const reveal = Math.max(0, Math.min(1, (value - .48) / .42));
    const daylightRaw = Math.max(0, Math.min(1, (value - .06) / .82));
    const daylight = daylightRaw * daylightRaw * (3 - 2 * daylightRaw);
    const scale = 3.65 - eased * 2.59;
    const phoneTop = 50 + eased * 18;
    section.style.setProperty('--display-layout-progress', eased.toFixed(3));
    section.style.setProperty('--display-copy-opacity', reveal.toFixed(3));
    section.style.setProperty('--display-daylight', daylight.toFixed(3));
    section.style.setProperty('--display-phone-scale', scale.toFixed(3));
    section.style.setProperty('--display-phone-top', `${phoneTop.toFixed(2)}%`);
  };

  const preload = Promise.all(Array.from({ length: count }, (_, index) => new Promise((resolve) => {
    const image = new Image();
    image.onload = image.onerror = () => resolve(image);
    image.src = frameUrl(index);
    frames[index] = image;
  }))).then(() => {
    ready = true;
    section.dataset.sequenceState = 'ready';
  });

  const playOnce = async () => {
    if (played) return;
    played = true;
    section.dataset.sequenceState = 'loading';
    await preload;
    if (reducedMotion) {
      sequence.src = frameUrl(count - 1);
      setSequenceProgress(1);
      section.dataset.sequenceState = 'ended';
      return;
    }
    section.dataset.sequenceState = 'playing';
    const startedAt = performance.now();
    const frameDuration = 1000 / 24;
    const render = (now) => {
      const index = Math.min(count - 1, Math.floor((now - startedAt) / frameDuration));
      const progress = index / (count - 1);
      const nextSrc = frames[index] && frames[index].src ? frames[index].src : frameUrl(index);
      if (sequence.src !== nextSrc) sequence.src = nextSrc;
      section.dataset.frame = String(index);
      setSequenceProgress(progress);
      if (index < count - 1) requestAnimationFrame(render);
      else {
        setSequenceProgress(1);
        section.dataset.sequenceState = 'ended';
      }
    };
    requestAnimationFrame(render);
  };

  let ticking = false;
  const updateScene = () => {
    const rect = section.getBoundingClientRect();
    const topOffset = innerWidth <= 600 ? 60 : 70;
    const visibleTrigger = innerHeight * .5;
    if (!played && rect.top <= visibleTrigger && rect.bottom > 0) playOnce();
    const scrollRange = Math.max(360, innerHeight * .72);
    const layoutProgress = (topOffset - rect.top) / scrollRange;
    setLayoutProgress(layoutProgress);
    ticking = false;
  };
  const requestSceneUpdate = () => {
    if (ticking) return;
    ticking = true;
    requestAnimationFrame(updateScene);
  };
  addEventListener('scroll', requestSceneUpdate, { passive: true });
  addEventListener('resize', requestSceneUpdate, { passive: true });
  setSequenceProgress(0);
  setLayoutProgress(0);
  updateScene();
  if (ready) section.dataset.sequenceState = 'ready';
})();
</script>
<script>
(() => {
  const gallery = document.querySelector('.ip17-motion-gallery');
  const prev = document.querySelector('[data-motion-prev]');
  const next = document.querySelector('[data-motion-next]');
  const indexLabel = document.querySelector('[data-motion-index]');
  if (!gallery || !prev || !next) return;
  const cards = Array.from(gallery.querySelectorAll('figure'));
  const controls = gallery.parentElement.querySelector('.ip17-motion-controls');
  const dots = document.createElement('div');
  dots.className = 'ip17-motion-dots';
  dots.setAttribute('aria-label', '幻灯片进度');
  const dotButtons = cards.map((card, index) => {
    const dot = document.createElement('button');
    dot.type = 'button';
    dot.setAttribute('aria-label', `第 ${index + 1} 张`);
    dot.addEventListener('click', () => goTo(index));
    dots.appendChild(dot);
    return dot;
  });
  controls.prepend(dots);
  let activeIndex = 0;
  let targetIndex = null;
  let settleTimer;
  const updateIndex = () => {
    const anchor = gallery.scrollLeft + parseFloat(getComputedStyle(gallery).paddingLeft);
    activeIndex = cards.reduce((best, card, index) => Math.abs(card.offsetLeft - anchor) < Math.abs(cards[best].offsetLeft - anchor) ? index : best, 0);
    if (indexLabel) indexLabel.textContent = String(activeIndex + 1).padStart(2, '0');
    dotButtons.forEach((dot, index) => dot.setAttribute('aria-current', index === activeIndex ? 'true' : 'false'));
  };
  const goTo = (index) => {
    targetIndex = Math.max(0, Math.min(cards.length - 1, index));
    const inset = parseFloat(getComputedStyle(gallery).paddingLeft);
    gallery.scrollTo({ left: cards[targetIndex].offsetLeft - inset, behavior: 'smooth' });
  };
  const settle = () => { clearTimeout(settleTimer); targetIndex = null; updateIndex(); };
  let scrollTicking = false;
  gallery.addEventListener('scroll', () => {
    clearTimeout(settleTimer);
    settleTimer = setTimeout(settle, 180);
    if (scrollTicking) return;
    scrollTicking = true;
    requestAnimationFrame(() => { updateIndex(); scrollTicking = false; });
  }, { passive: true });
  gallery.addEventListener('scrollend', settle);
  gallery.addEventListener('pointerdown', () => { targetIndex = null; }, { passive: true });
  gallery.addEventListener('wheel', () => { targetIndex = null; }, { passive: true });
  prev.addEventListener('click', () => goTo((targetIndex ?? activeIndex) - 1));
  next.addEventListener('click', () => goTo((targetIndex ?? activeIndex) + 1));
  updateIndex();
})();
</script>
