# 作品集网站设计与交互规范

本文件用于约束后续作品集页面的设计、内容拆分与开发实现。目标是在面试演示场景中建立专业、清晰、统一的浏览体验，并避免后续修改重新引入已经否定的设计。

## 设计目标

- 页面首先服务于面试官快速理解项目，其次才是视觉表现与技术展示。
- 每一屏只表达一个核心结论，形成“背景—问题—职责—过程—验证—成果”的连续叙事。
- 视觉气质保持克制、专业、精致，参考 Apple 官网的字号层级、留白与滚动节奏，但不机械照搬具体样式。
- 保留项目自身品牌特征：得物使用青绿色与数字资产语言，幻星使用紫粉与虚拟角色语言，嘟记豆使用轻松、温暖的产品与品牌语言。
- 技术与交互必须服务信息表达，禁止为了显得复杂而堆叠标签、光效和动画。

## 展示原则

- 页面首先服务于招聘者和作品阅读者，不展示开发过程中的辅助标记。
- 正式页面不出现 `ASSET`、`UI-01`、`SHOE-01`、`HOVER / MOVE`、`WEB ELEMENTS`、素材尺寸、替换说明等开发标签。
- 素材编号、推荐尺寸和替换规范只保存在 `assets-source` 内的说明文档中。
- 不为显而易见的交互增加文字说明；可点击性通过位置、层级、光标和动效表达。
- 装饰性英文、标签或技术词只有在强化项目叙事时才保留，不用于填补空白。
- 关键过程章节在桌面演示模式下必须尽量适配单屏可视高度：标题、说明、卡片和控制项需要在一个浏览器视口内完整呈现；短屏和移动端改用自然文档高度，禁止为了单屏效果裁切内容。
- 项目章节统一使用纵向标题层级：章节编号、单行主标题、左对齐副标题、核心内容；不再把主标题与说明拆到页面左右两端。
- 桌面端章节标题、说明和内容模块共用相同的左右边线。主标题优先精简文案并保持单行，不以过度缩小字号换取单行。
- 桌面浏览器高度不低于 721px 时，章节高度使用扣除导航后的单屏高度；更矮窗口及移动端恢复自然高度与正常换行。

## 项目叙事结构

项目页原则上按照以下顺序组织，可根据项目性质增删，但不得退回“整张图片依次堆叠”的方式：

1. 项目首屏：项目名称、核心价值、角色与项目属性。
2. 项目背景：业务机会、用户问题和项目发生的原因。
3. 项目目标：用一句话说明要实现的体验或业务改变。
4. 我的职责：明确本人负责的范围、关键判断和跨团队协作方式。
5. 解决过程：策略、流程、系统或设计迭代，不只展示最终画面。
6. 技术与效率：只在确实产生项目价值时展示 AI、自动化或工具能力。
7. 品质验证：通过前后对比、场景浏览或测试结果说明改进。
8. 项目成果：数据结果、用户价值、业务价值和可沉淀资产。

- 原始 PDF 或长图只作为内容来源，不直接决定网页排版。
- 长图中的标题、说明、数据、步骤和卡片应优先还原为 HTML 元素。
- 只有难以拆分的角色视觉、UI 截图、模型效果和宣传海报保留为图片。
- 没有独立素材时允许使用比例正确的占位图，但必须保持后续可直接替换的独立结构。

## 单屏与滚动规则

- 桌面端以 `100svh - 导航高度` 作为章节高度基准，并针对 1280×720、1440×900、1920×1080 检查完整性。
- 章节内容不得因为固定高度而被裁切；内容过多时应先精简文案、缩短间距、调整图片占比，再考虑恢复自然高度。
- 普通章节不使用 `sticky` 或吸顶。用户滚动时必须保持自然连续，不得无故停顿或卡住。
- 只有明确需要层级覆盖叙事的章节允许固定画面，例如商品浏览体验；固定区域必须由前后章节自然覆盖并退出。
- 全屏背景图优先保持 16:9 视觉构图，允许使用 `object-fit: cover`，但需要针对主体位置设置 `object-position`。
- 标题卡、评论气泡和装饰元素不能遮挡图片主体、手机 UI 或关键内容。
- 桌面端控制为单屏不等于强制滚动吸附；除非项目明确需要，不使用 `scroll-snap`。

## 文字与信息层级

- 层级顺序固定为：章节编号 → 英文或类型提示 → 主标题 → 副标题/说明 → 内容模块。
- 主标题和副标题统一左对齐，不放在页面左右两端形成视觉分散。
- 主标题应先提炼结论，避免同时塞入背景、方法和结果；通常控制在 12–24 个汉字。
- 副标题与主标题之间必须有明确间距与字号差，不得看起来像连续正文。
- 正文以容易阅读为第一目标：桌面端一般不小于 15px，重要说明建议 17–20px，卡片说明一般不小于 13px。
- 禁止通过过度缩小文字制造“高级感”。项目背景、目标、职责等核心信息必须能够在演示距离下直接阅读。
- 控制文本栏宽，避免过长行；中文正文建议每行约 24–38 个汉字。
- 调整文案或容器宽度，避免标题和段落末行只剩单个字、单个词或孤立标点。
- 中文主标题中的完整短语尽量不拆行，例如“3D 设计负责人”“用户价值”保持整体。
- 数据数字与单位适度拉开，数字为视觉主体，单位和说明降低一级。

## 网格与对齐

- 同一章节的编号、标题、说明和内容区域使用统一左右边线。
- 双栏排版必须视觉居中，并校准左右内容的顶部、底部与总体高度，禁止一侧明显悬空或偏上。
- 项目背景与项目目标并列时，两侧内容模块应使用一致的高度、分隔线和上下结构。
- 序号需参与整体排版：大序号高度应与右侧标题和说明整体接近，而不是缩成装饰小字。
- 流程节点与箭头均匀分布；所有节点之间的箭头间距一致，首尾节点与容器边缘保持平衡。
- 卡片宽度应与标题内容区整体左右对齐，不得擅自缩小卡片并放大间距。
- 调整局部时保持用户已经确认的卡片比例、宽度与排版范围，避免连带修改无关尺寸。

## 桌面端与移动端协调规则

- 多端一致指信息、品牌气质、视觉重心和操作结果一致，不要求把桌面布局等比例缩小到手机。桌面端负责建立完整空间关系，移动端按阅读顺序重组，但不能缺少核心内容或导航入口。
- 响应式断点统一按内容承载能力设置：`1024px` 以下收紧外边距与复杂网格，`820px` 以下进入平板/移动布局，`520px` 以下进入窄屏手机布局。新增局部断点前必须确认现有断点无法解决问题，避免规则相互覆盖。
- 页面容器、图片和交互组件必须使用流式尺寸；优先使用 `clamp()`、百分比、`minmax()`、`aspect-ratio` 与合理的 `max-width`，避免只适配单一设备的固定像素宽高。
- 桌面与移动端保持相同的信息层级和阅读顺序。双栏在移动端转为单栏时，顺序应为标题与结论、核心视觉、补充说明；禁止因为视觉调整造成语义顺序颠倒。
- 顶部导航在所有端保留“关于 / 项目 / 联系”等核心入口。空间不足时先缩小间距、字号和 Logo 辅助文字，不得直接隐藏核心导航项；导航文字禁止换行。
- 移动端触控目标建议不小于 `44×44px`。仅用于文本跳转的导航可适当紧凑，但必须保留足够间距，避免相邻项目误触。
- 视觉主体与设备模型、背景标记组合时，必须按照主体的视觉中心对齐，不能只用统一的 `top`、`bottom` 或底边对齐推算。透明 PNG 需要同时考虑画布留白、实际可见区域、缩放中心与变换原点。
- 主视觉在桌面、平板和手机端分别校准位置。修改移动端位置时使用断点内的明确覆盖，不得依赖桌面端多轮遗留规则叠加；同一属性在一个断点内应尽量只有一个最终生效值。
- 同组及跨章节复用的卡片必须共享同一比例、宽度、圆角、内边距和图片裁切规则。网格可从桌面四列转为平板两列、手机单列，但卡片自身比例不得随章节变化。
- 手机单列卡片应设置合理的 `max-width` 并水平居中，避免在宽手机上被无限拉高；平板双列统一使用 `minmax(0, 1fr)`，防止内容最小宽度撑破网格。
- 移动端取消依赖 Hover 的灰度恢复、说明展开或关键信息显隐。触控设备默认显示可读状态；Hover 仅作为支持精细指针设备的增强效果。
- 桌面端单屏章节在移动端必须恢复自然高度。禁止保留导致文字裁切、卡片溢出或上下区域跳动的固定高度；复杂 Sticky 叙事需要提供移动端自然滚动方案。
- 移动端不以“塞进一屏”为目标。优先保证字号、触控、图片比例和段落节奏，再控制页面长度；不得通过缩小到不可读字号来复制桌面密度。
- 响应式修复完成后必须检查 CSS 优先级与源码顺序，确认旧媒体查询没有重新覆盖新规则；不能只根据桌面浏览器缩窄窗口的单次结果判断完成。

## 素材使用

- 优先使用用户提供的原始品牌标记、Logo、UI 和透明 PNG，不使用 CSS 临摹替代正式素材。
- 图片保持原始宽高比，除非用户明确要求裁切或变形。
- 页面元素保持独立，避免重新拼成不可编辑的整张长图。
- 占位符仅用于素材尚未提供的阶段；正式素材到位后应删除占位符及其文字。
- 页面展示使用清晰原图；正式高清素材到位后替换并删除模糊压缩版，避免长期保留重复资源。
- 竖屏 UI、手机截图或对比图不得为了填满横屏而强行拉伸，应利用原图上下安全区、左右对称关系或独立容器完成排版。
- 图片内已有的 UI、鞋款、角色等元素如果后续需要交互，应拆成独立素材，而不是继续对整张合成图做伪交互。
- 正式图片必须提供有意义的 `alt`，装饰图使用空 `alt` 或隐藏于辅助技术。

## 交互动效

- 动效应强化空间层级和内容理解，避免为了展示技术而增加交互。
- 列表切换优先保持运动方向连续，不让元素从一端突然跳到另一端。
- Hover 动效应轻微、可恢复，并尊重 `prefers-reduced-motion`。
- 移动端不依赖 Hover 才能理解内容或完成操作。
- 全站卡片 Hover 统一使用鼠标位置驱动的柔和渐变提亮，参考首页项目卡和职业经历；不使用固定在右下角的半透明亮点。
- Hover 反馈可组合轻微上浮、图片约 1.02–1.04 倍缩放、局部色彩恢复和标题颜色高亮，但避免同时叠加大角度旋转、粗描边和重投影。
- 页面进入动画以轻微上移和渐显为主，进入视口后自动完成；标题不应持续绑定滚动进度而反复出现或消失。
- 连续移动使用稳定且柔和的路径。匀速漂浮可使用 `linear`，状态切换使用 `cubic-bezier(.22, 1, .36, 1)`。
- 动画优先使用 `transform` 和 `opacity`，避免频繁修改布局属性造成卡顿。
- 快速点击或跨项选择必须直接响应目标项，不播放中间项停顿动画。
- 所有动画在 `prefers-reduced-motion` 下取消非必要位移、缩放和循环。

## 图片信息卡

- 信息卡默认可使用黑白或低饱和图片建立统一节奏，Hover 时恢复彩色，以强化可探索性。
- 图片上的文字必须放在稳定的渐变安全区内；使用由下向上的深色渐变拉开图文对比，禁止文字直接压在复杂高对比背景上。
- Hover 统一采用轻微上浮、图片小幅放大和阴影增强，不叠加夸张旋转或无意义装饰。
- 图片本身已能形成明确边界时不额外添加描边；层级优先通过间距、圆角、渐变和阴影表达。
- 推荐使用约 500–700ms 的柔和减速曲线 `cubic-bezier(.22, 1, .36, 1)`，与网站其他交互保持一致。
- 触控设备保留清晰的默认文字对比，不以 Hover 作为读取内容的前提；`prefers-reduced-motion` 下取消位移和缩放。

## 卡片与列表

- 卡片不默认添加描边。只有在深色背景上确实无法辨识边界时，才使用低对比度细线。
- 项目挑战、流程迭代等同类内容必须使用一致的图片比例、卡片尺寸、圆角、文字安全区和 Hover 规则。
- 卡片标题和说明采用上下结构；需要突出序号时，序号与标题说明组成一个整体，不单独漂浮。
- 列表 Hover 参考首页职业经历：左侧强调线或局部渐变出现，内容轻微横移，标题变为项目强调色。
- 同一列表内所有子项的 Hover 形式必须一致，不允许不同项使用不同背景、位移或高亮方式。
- 卡片内不添加无法解释用途的英文角标、素材编号、替换提示或装饰按钮。

## 项目背景、目标与职责

- “项目背景”和“项目挑战”是不同章节：背景解释为何做，挑战解释具体阻力，不得混用标题。
- 项目目标使用清晰价值结论，不堆叠空泛口号；用户、业务、品牌等价值项应保持统一结构。
- 背景与目标双栏布局必须垂直居中、整体等高，并通过一致的列表结构建立左右平衡。
- 子项标题和说明采用稳定的上下结构，标题优先表达问题或价值，说明补充结果或原因。
- “我的职责”必须回答三件事：我负责什么、我做了哪些关键判断、我如何推动跨团队落地。
- 职责内容优先拆为 3–4 个清晰工作流，并补充协作链路；避免只写宽泛职责词或大段自述。
- 职责章节中的角色名称保持单行，卡片序号高度应与标题和说明整体协调。

## 前后对比与成果页

- 前后对比优先建立清晰对称关系：左侧 Before，右侧 After，中间只保留必要的对比连接元素。
- 竖屏截图需要尽量放大有效内容，压缩无意义的上下空白；可利用截图自身的深色安全区放置 Before、After、3D Mesh、3DGS 等标签。
- 对比标题不必单独占一整屏，应与对比内容形成连续章节，确保核心图片拥有足够面积。
- 前后图片 Hover 可以显示与差异有关的磨砂信息卡，例如“塑料感”“更加真实”；不添加跟随鼠标的大面积模糊高亮。
- 项目成果页优先突出结果图片和关键数据。文字数据 Hover 使用与首页经历一致的局部高亮，不遮挡主视觉。
- 成果图片按照实际比例设计版面，避免某一侧存在大量无意义上下空白。

## 得物案例首屏

- 中心鞋为唯一选中态，默认逆时针旋转 20°。
- 左右鞋保持水平角度，只做位置、尺寸、透明度变化。
- 最外侧鞋为中心鞋的约 60%；离开展示范围后沿原方向淡出，在不可见状态下重排并从另一端淡入，实现无限切换，但禁止可见的头尾瞬移或横穿舞台。
- 手机 UI、POIZON Logo 和“得到运动”标记使用正式图片素材。
- 不显示鞋号、分页、切换箭头或交互提示文字。
- 中心选中鞋与两侧鞋必须避免交叠；中心鞋保持既定尺寸，两侧鞋独立调整。
- 两侧鞋保留向外逐级缩小和半透明衰减，底部视觉对齐。
- 两端元素切换时从画外沿运动方向淡入，不能在原地突然出现。
- 无限循环必须在视觉上连续；不可只运行一轮，也不可让首尾元素可见地横穿舞台。
- 鞋款 Hover 的陀螺仪效果只作用于当前选中鞋，角度轻微、响应及时且回弹柔和。
- 手机端鞋组需要与手机模型的可见主体中心对齐；定位时根据鞋图与手机图的实际素材比例计算，不以桌面端底部基线直接换算。
- 得物首屏至少分别校准桌面、平板和窄屏手机三个区间。移动端调整鞋组高度时不得连带改变手机、Logo 和底部品牌标记的既定比例。

## 得物 3D 浏览体验

- 鞋类与手办使用连续的全屏滚动叙事：鞋类画面向上离场并揭示下层手办画面，随后由项目成果向上覆盖；不使用标签或按钮切换。背景保持原始 16:9 构图，评论和互动图标必须作为独立网页元素。
- 评论卡只保留少量循环项，从手机两侧交错上浮并淡入淡出；使用半透明磨砂玻璃与柔和渐变，不同时堆叠过多内容。
- 鞋类鼠标粒子使用正式点赞图标，手办使用正式爱心图标；粒子由大到小向上漂浮，禁止高频喷射或遮挡手机主体。
- 评论动画只在章节进入视口后运行；切换品类时同步切换评论、背景和鼠标粒子素材。
- 评论卡使用纯圆角磨砂玻璃样式，不加投影；头像、昵称与评论文字必须清晰可读。
- 评论卡从手机左右两侧中下区域开始渐显并匀速上浮，在接近屏幕顶部时渐隐，不从浏览器底部进入，不在中途停顿。
- 左右评论卡位置需要适度错开，层级高于左上角标题卡。Hover 某条评论时只暂停该条，其他评论继续运动。
- 鼠标粒子的横向漂移使用连续正弦或平滑噪声路径，禁止锯齿式左右跳动。
- 粒子数量、初始尺寸和速度以“能够感知但不遮挡内容”为准；发射频率需经过真实页面检查，不按单个参数孤立判断。

## 哔哩哔哩幻星案例

- 不再使用原始 16:9 页面截图连续堆叠，统一拆分为：项目背景、目标路径、我的职责、AI 提效、3D 生产管线、产品体验、IP 角色视觉、项目成果。
- 原始图片中的标题、说明、数据、阶段和流程需转成 HTML；角色海报、界面效果和制作过程画面可作为局部高清视觉素材。
- 视觉强调色使用紫粉与青色，背景保持深色科技感，但不使用过量霓虹描边。
- 角色海报使用统一矩阵与比例，Hover 采用轻微上浮、图片放大和鼠标位置渐变提亮。
- AI 章节强调“如何提高概念探索和沟通效率”，不得只罗列工具名称。
- 生产管线强调从原画、模型材质、绑定表情、引擎配置到效果验收的连续关系。
- IP 孵化章节必须同时表达角色定位、视觉体系和可持续内容资产，而不是只展示漂亮角色图。

## 开发验收清单

每次完成或修改项目页后至少检查以下内容：

- 1280×720 桌面视口下，关键章节是否完整显示且没有裁切。
- 1440×900 与 1920×1080 下，标题、卡片和图片是否仍保持合理占比。
- 768px 平板宽度下，双栏与多列内容是否按预期重组，图片主体是否仍与设备或容器居中。
- 390×844、430×932 和约 500px 宽手机视口下，导航是否完整、文字是否正常换行、卡片是否等宽等比、页面是否存在横向溢出。
- 移动端是否恢复自然高度，文字和操作是否不依赖 Hover，触控状态是否直接呈现完整信息。
- 所有图片是否加载成功、清晰且保持正确比例。
- 主标题、副标题、正文是否具有明确层级，是否存在孤字或异常换行。
- 同类卡片的尺寸、间距、对齐和 Hover 是否一致。
- 页面是否出现无意义开发标签、素材说明、占位文案或多余箭头。
- 滚动时是否存在意外吸顶、卡顿、跳帧或动画响应延迟。
- 快速点击和跨项选择是否能直接到达目标状态。
- `prefers-reduced-motion` 下是否仍能完整理解内容。
- 浏览器缩放模拟检查后，至少再使用一台真实手机或等效设备模式复核安全区、浏览器地址栏引起的视口高度变化及触控交互。

## 明确禁用项

- 禁止重新把已拆分的内容合成整张长图展示。
- 禁止未经要求改变用户已确认的卡片宽度、比例和整体左右边线。
- 禁止用极小字号、大面积空白或稀疏排版伪装高级感。
- 禁止给所有章节添加吸顶、滚动锁定或滚动吸附。
- 禁止添加粗描边、重投影、固定亮点和无意义装饰英文。
- 禁止在正式页面展示“Hover / Move”“Web Elements”“素材清单”“建议尺寸”等开发提示。
- 禁止为了填满容器拉伸图片或破坏 UI、手机、角色和产品的原始比例。
